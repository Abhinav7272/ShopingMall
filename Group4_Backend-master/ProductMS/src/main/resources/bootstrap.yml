server:
  port: 3334
  
spring:
  application:
    name: ProductMS
  cloud:
    consul:
      host: localhost
      config:
        fail-fast: false
        enabled: true
        prefix: config
        defaultContext: application
        data-key: data
        profileSeparator: '::'
        format: YAML 
