server:
  port: 3338
  
spring:
  application:
    name: PaymentMS
  cloud:
    consul:
      host: localhost
      config:
        fail-fast: false
        enabled: true
        prefix: config
        defaultContext: application
        data-key: data
        profileSeparator: '::'
        format: YAML 
